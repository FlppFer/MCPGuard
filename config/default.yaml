scope: "default"
log-level: "debug"

data_base:
  mock: true

object_storage:
  mock: true
  base-path: "mcpguard-local-storage"

# Authentication - disabled by default for local development
auth:
  enabled: false
  api_keys: []